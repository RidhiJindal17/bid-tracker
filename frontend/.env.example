# The endpoint URL of the backend API gateway.
# For decoupled hosting (e.g. Vercel), point this to your production backend host.
# For unified hosting (e.g. Render/Railway serving frontend dist), this can be left blank.
VITE_API_URL=http://localhost:5000/api
